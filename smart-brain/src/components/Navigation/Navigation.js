import React from 'react';

const Navigation = () => {
    return(
        <nav style = {{display: 'flex', justifyContent: 'flex-end'}}>
            <p className = 'f3 link dim pa3 ba2 br-pill pointer shadow-5'> Sign Out </p>
        </nav>
    );
}

export default Navigation;